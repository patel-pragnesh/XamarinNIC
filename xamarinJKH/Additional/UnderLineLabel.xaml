<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="xamarinJKH.Additional.UnderLineLabel">
  <ContentView.Content>
        <StackLayout  Grid.Row="0" Padding="0" VerticalOptions="Center">
            <Label x:Name="customLabel" Text="{Binding}" TextColor="{Binding TextColor,Mode=TwoWay}"
                   VerticalTextAlignment="Center" HorizontalTextAlignment="Start" FontSize="Medium" Margin="5"/>
            <BoxView x:Name="customBox"   
                    BackgroundColor="{Binding LineColor,Mode=TwoWay}"  
                    HeightRequest="1"   
                    Margin="0,-8,0,0" />
        </StackLayout>
    </ContentView.Content>
</ContentView>